# 2026-07-28 Production 출시 기록

## 출시 기준

- 배포 기준 브랜치: `main`
- 배포 기준 커밋: `9cc5327ce875afeef8cedbf8ba392cb4613fe0d4`
- Production 도메인: `https://wondesign.studio`
- 운영 문의 수신: `wondesign01@gmail.com`
- 상담 전화: `010-3991-9389`
- 기본 응답 기준: 1영업일 이내
- 문의 데이터 보관: 접수일로부터 12개월

비밀 키와 문의 원문은 이 문서에 기록하지 않습니다.

## 완료 결과

### GitHub·Vercel

- Draft PR #1을 squash merge하여 `main`에 반영했습니다.
- 기존 Vercel `wds-website` 프로젝트를 `wondesignstudio/WDS-website` 저장소와 연결했습니다.
- Production branch를 `main`으로 유지했습니다.
- Production 환경변수 13개를 운영 값으로 갱신하고 새 빌드를 완료했습니다.
- 배포 상태 `Ready`와 custom domain 할당을 확인했습니다.

### DNS·HTTPS

- GoDaddy 기본 nameserver로 전환했습니다.
  - `ns61.domaincontrol.com`
  - `ns62.domaincontrol.com`
- 현재 웹 레코드는 다음과 같습니다.
  - root `A`: `216.198.79.1`
  - `www` `CNAME`: `f4ce2d65b3f9f17f.vercel-dns-017.com.`
- `wondesign.studio`는 Vercel `Valid Configuration`입니다.
- `www.wondesign.studio`는 root로 HTTP 308 리디렉션됩니다.
- root HTTPS, HSTS, canonical, `robots.txt`, `sitemap.xml`을 확인했습니다.
- 전환 전 Hostcocoa nameserver와 기존 root·www A 레코드는 롤백 자료로 보존했습니다.

### Resend·GA4

- Resend의 DKIM·SPF·MX 검증 상태는 `verified`입니다.
- 발신자는 `WDS <contact@wondesign.studio>`입니다.
- 운영자 알림과 사용자 자동회신은 `wondesign01@gmail.com`에서 실제 수신을 확인했습니다.
- GA4 Measurement ID는 기존 WDS 속성의 `G-XSDLQBM9EJ`를 연결했습니다.

### Production E2E

- 허용된 `wondesign01@gmail.com` 계정으로 custom domain 관리자 Google 로그인을 확인했습니다.
- 합성 문의 1건을 Production에서 제출했습니다.
- 문의 접수 화면, Supabase 저장, 관리자 목록·상세 조회를 확인했습니다.
- 내부 알림과 사용자 자동회신은 각각 `sent`, 시도 1회로 처리됐습니다.
- Gmail 받은편지함에 두 메일이 같은 시각에 도착했습니다.
- Vercel에서 `/api/contact` 요청이 HTTP 201로 완료됐습니다.
- 해당 요청의 운영 로그에는 문의 원문, 이메일 주소, 비밀 환경변수 이름·값이 남지 않았습니다.

## 공개 경로 확인

- `/`, `/work`, `/work/questboard`, `/contact`, `/privacy`: 정상
- 브랜드 404 화면: 정상
- `/terms`: 법적 문서가 아직 발행되지 않아 404 유지

프로젝트 이미지·고객 로고·성과 주장·추천사와 최종 법적 문서 발행은 공개 콘텐츠 승인 단계에서 진행합니다.

## 후속 확인

2026-07-29에 다음 항목을 다시 확인합니다.

- DNS와 HTTPS
- root·`www` 리디렉션
- Resend 도메인 인증과 메일 수신
- 이메일 재처리 cron
- GA4 수집 상태
- 문의 저장·관리자 접근 상태
- Vercel 오류 로그

